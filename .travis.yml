language: go
go:
- 1.3

before_install:
- vagrant/provision.sh
- kafka/bin/kafka-topics.sh --create --partitions 1 --replication-factor 3 --topic single_partition --zookeeper localhost:2181
- kafka/bin/kafka-topics.sh --create --partitions 2 --replication-factor 3 --topic multi_partition --zookeeper localhost:2181

notifications:
  flowdock: 15e08f7ed3a8fd2d89ddb36435301c1a
